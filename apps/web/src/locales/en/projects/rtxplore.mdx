---
title: RTXplore
description: This project focuses on the development of a graphics engine that integrates both ray tracing and rasterization rendering techniques.
tags: ["RayTracing", "Graphics", "Architecture", "DirectX12"]
status: Maintenance
---

import ProjectLayout, {
  ProjectImage,
  ProjectTag,
  GitHubLink,
  WarningBox,
} from "../../../components/ProjectLayout";

export const imagesPath = "/src/assets/rtxplore";
export const imagesComparisonPath = "/src/assets/rtxplore/Comp";

# What I Built

I designed and implemented a small graphics engine that mixes **rasterization** and **ray tracing** under a single architecture: one scene, two complete pipelines, shared resource management, and comparable visual stages.

The key focus areas (implemented):

- **Engine infrastructure** with DX12 and Windows API: window swapchain, resource views (RTV/DSV/CBV/SRV/UAV), PSO + Root Signatures, shader compilation in HLSL.
- **Scene content pipeline**: procedural terrain + water, skybox and fog to exercise all stages.
- **Rasterization system**: tessellation/displacement, normal mapping, texture arrays and PCF shadow mapping pass.
- **Ray tracing system**: TLAS/BLAS, shader tables, closest-hit/any-hit/miss + **procedural** primitives via AABBs and an Intersection Shader.
- **Reflections & refractions**: dynamic cubemap with geometric correction (raster) and recursive rays (RT).
- **CPU–GPU sync and profiling**: Nsight Graphics & Aftermath; tests on RTX 4070 (5888 CUDA, 46 RT cores, 12 GB, 504 GB/s).
- **Effect parity**: identical scene & materials to expose visual and performance trade-offs across both pipelines.

<GitHubLink
  url="https://github.com/BogSav/RTXplore"
  label="You can find the project on my github here."
  iconSize="large"
/>

---

# Effects Gallery

Let's jump straight into the visual results achieved in this projects (since that's the main point of a graphics engine).
In a further section I will show you the direct comparison between ray tracing and rasterization for the same scene and materials.

## The Scene

- Example of real-time water caustics (reflexions and refractions) using rasterization. The
  reflection of the skybox on the water’s surface can be observed in the distance, while the
  refracted rocks from the lake’s bottom can be seen up close.

<ProjectImage
  src={`${imagesPath}/RasterizationReflexionsRefractions.png`}
  alt="Raster reflections (env map / SSR)"
  caption="Water caustics using rasterization."
/>

## Ray Tracing Effects

In ray tracing, the process of achieving reflections and refractions is straightforward and
natural, based on a technique called recursive ray tracing. When an intersection is reported
to a closest hit shader the reflection and refraction vectors are calculated within
the shader. Rays are then launched into the scene in the directions of these vectors.
These rays return the captured colors, which are then combined with diffuse lighting and
shadowing.

- Example of reflections using ray tracing; the water surface accurately reflects the skybox and
  surrounding environment with correct perspective and angle-of-incidence effects.

<ProjectImage
  src={`${imagesPath}/RayTracingReflexions.png`}
  alt="Ray-traced reflections on water"
  caption="Example of reflections using ray tracing."
/>

- Example of refractions using ray tracing; the water surface accurately refracts the rocks
  at the bottom of the lake, bending light according to Snell's law and preserving shoreline geometry.

<ProjectImage
  src={`${imagesPath}/RayTracingRefractions.png`}
  alt="Ray-traced refractions"
  caption="Example of refractions using ray tracing."
/>

---

# Ray Tracing vs. Rasterization

In this section, I present a comparative analysis of rasterization and ray tracing by rendering
the same scene using both methods. This analysis explores the differences and
similarities in the visual effects produced by each technique, as well as examines the resources
utilized, such as memory usage and computational load.

## Visual Differences

Although the scene looks geometrically identical in both rasterization and ray tracing,
notable differences emerge in global illumination and texturing details. These differences
arise from the fundamentally different approaches each method uses to handle drawing
to screen and lighting.

- Ray tracing images show **noise** when using 1 spp; rasterization stays clean at similar cost, as can be seen in the image below. A denoiser (e.g., RTDAO) would mitigate it.

<ProjectImage
  src={`${imagesComparisonPath}/Texture.png`}
  alt="Specular/roughness response"
  caption="Noise comparison. Rasterization (left) is clean; RT (right) shows noise at 1 spp."
/>

- With **big waves**, RT can look overly transparent at grazing angles (angle-of-incidence effect); raster env-map hides some of that but remains approximate. Depth-aware correction helps both.

<ProjectImage
  src={`${imagesComparisonPath}/BSW.png`}
  alt="Big/Small waves sheet"
  caption="Big scale waves comparison."
/>

- Near **shore edges**, RT refractions correctly bend shoreline geometry; rasterization
  with env-map struggles unless SSR or dynamic cubemap centering is used.

<ProjectImage
  src={`${imagesComparisonPath}/Refractions.png`}
  alt="Raster vs Ray tracing refractions comparison"
  caption="Refractions comparisson."
/>

Key takeaways:

- rasterization wins for throughput and predictable cost; great for live interaction and broad hardware.
- RT wins for correctness of reflection/refraction at grazing angles, edge cases, and multi-bounce paths.
- My engine lets you toggle the pipeline while keeping the same scene and materials, so differences are visible—not theoretical.

## Performance Comparison

To analyze the performance of my rendering engine, I utilized NVIDIA’s debugging and
profiling tools. Specifically, I used NVIDIA Nsight Aftermath Monitor to track GPU performance metrics such as frame time, memory usage, and shader execution.

- In the image below you can see a rendering pass in rasterization first - notably you can see first the shadow mapping pass, the 6 dynamic cube mapping passes and, at the end, the main rendering pass.

<ProjectImage
  src={`${imagesComparisonPath}/RastSpec.png`}
  alt="Performance comparison raster vs ray tracing"
  caption="Rasterization profiling using the GPU Trace Profile of the NVIDIA Nsight Graphics software.
Panel (a): shows the timeline of a registered frame along with various informations
about the resources used, data transfered and the graphics commands. Here can be set
a timestamp to analyze the metrics at one specific point in time. Panel (b): presents the
metrics at one specific timestamp. Panel (c): shows the executed hardware instructions at
one specific timestamp. Panel (d): shows what shaders and shader functions were called
at one specific timestamp."
/>

- In the image below you can see a rendering pass in ray tracing.
  Profiling tools offer limited information for ray tracing compared to rasterization
  due to the asynchronous nature of the rendering execution. Each ray generation shader
  effectively follows its path, leading to recursive ray tracing processes. This complexity
  means detailed, stage-by-stage analysis is less feasible.

<ProjectImage
  src={`${imagesComparisonPath}/RTSpecs.png`}
  alt="Performance comparison raster vs ray tracing"
  caption="Ray tracing profiling"
/>

## Some Real Performance Metrics

- **Rasterization**

Finally, I assessed the framerate, which is heavily influenced by the graphics card
driver and refers to the number of Frames per Second (FPSs) displayed. The tests and
metrics were obtained using Game Ready Driver version 555.99 for the NVIDIA RTX 4070 graphics card.
With dynamic cube map updates occurring in every frame, an average
framerate of 1700 ± 10% FPS was achieved. When the dynamic cube map was updated
every 0.1 seconds, the framerate increased to 3300 ± 15% FPS.

- **Ray Tracing**

Regarding framerate, I achieved approximately 230 FPS ± 30%. This framerate
can vary drastically depending on the camera’s position. If the camera is very close to
an object, the BVH traversal heuristic algorithms can more efficiently find the intersected
geometry compared to when the camera views the entire scene. This efficiency arises
because closer objects result in shorter traversal paths within the BVH, reducing the
computational load. Conversely, viewing the entire scene involves more extensive BVH
traversal, increasing the rendering time.

---

# Technical Highlights

## Procedural Terrain (Height Map)

- Terrain generated with fractal **fBm** over 2D **Simplex noise** (coherent; deterministic), then scaled to realistic heights.
- Mesh is split into **chunks** with AABBs for **frustum culling**; only visible submeshes are drawn.
- The image below shows the generated height map used for the terrain.

<ProjectImage
  src={`${imagesPath}/TerrainHeightMap.png`}
  alt="Height map"
  caption="Fractal fBm (Simplex) height generation → chunked mesh + AABB per chunk for frustum culling."
/>

## Water: Small vs Large Waves

- Tessellation/domain stage computes displaced positions and TBN; pixel stage applies normal/diffuse sampling (terrain uses arrays with height-based blending; water uses uniform color + normal).

<div className="grid grid-cols-1 md:grid-cols-2 gap-12">
  <ProjectImage
    src={`${imagesPath}/SmallWaves.png`}
    alt="Small waves"
    caption="Small-scale ripples via normal/displacement mapping; animated over time."
  />
  <ProjectImage
    src={`${imagesPath}/LargeWaves.png`}
    alt="Large waves"
    caption="Large-scale waves via analytic model; stable TBN from ∂P/∂x, ∂P/∂z."
  />
</div>

## Shadows

- **Shadow mapping** pass with orthographic light camera, depth bias and **PCF** filtering to soften edges.
- In RT path, a **shadow ray** + boolean payload determines occlusion via miss-shader logic.

<ProjectImage
  src={`${imagesPath}/ShadowMap.png`}
  alt="Shadow map"
  caption="Depth-only pass + PCF; separate PSO and biasing tuned for mountainous terrain."
/>

## Ray–Surface Intersection (Procedural)

- Procedural water in RT uses **AABB primitives** and an **Intersection Shader** to compute the exact displaced hit before closest-hit shading.

<ProjectImage
  src={`${imagesPath}/IntersectionShader2.png`}
  alt="Intersection shader"
  caption="DXR procedural: AABB hit → exact surface intersection in Intersection/Closest-Hit."
/>

## Reflections & Refractions — how each pipeline does it

- **Rasterization:** dynamic cube-mapping from six 90° cameras; refraction via env-map + geometric correction.
- **Ray tracing:** recursive rays in closest-hit; colors from reflection/refraction rays are combined with direct light and shadows.

---

# Future Work

- Implement a denoiser (e.g., RTDAO) to clean up noise in ray-traced images at low samples per pixel.
- Add more complex materials (e.g., metals, translucency) and support for IOR variations.
- Optimize performance further by exploring advanced acceleration structures and ray traversal techniques.
- Add shader hot swapping in debug and shader precompiling in release, for faster iteration times.
- Integrate additional global illumination effects, such as ambient occlusion and soft shadows.
- Explore hybrid rendering techniques that combine the strengths of both rasterization and ray tracing - such as deferred hybrid path tracing.

<WarningBox
  title="If you are interested in doing something together in this area (as a side project or research collaboration), feel free to reach out!"
  severity="error"
/>

export default ({ children }) => (
  <ProjectLayout frontmatter={frontmatter}>{children}</ProjectLayout>
);
